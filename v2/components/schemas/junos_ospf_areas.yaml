description: Junos OSPF areas
properties:
  include_loopback:
    default: false
    type: boolean
  networks:
    additionalProperties:
      properties:
        auth_keys:
          additionalProperties:
            type: string
          description: if `auth_type`==`md5`. The property key is the key number
          type: object
        auth_password:
          description: if `auth_type`==`password`, the password, max length is 8
          type: string
        auth_type:
          default: none
          description: auth type
          enum:
          - none
          - md5
          - password
          type: string
        bfd_minimum_interval:
          example: 500
          maximum: 255000
          minimum: 1
          type: integer
        dead_interval:
          maximum: 65535
          minimum: 1
          type: integer
        hello_interval:
          maximum: 255
          minimum: 1
          type: integer
        interface_type:
          default: nbma
          description: interface type (nbma = non-broadcast multi-access)
          enum:
          - nbma
          - p2p
          - p2mp
          type: string
        metric:
          example: 10000
          maximum: 65535
          minimum: 1
          nullable: true
          type: integer
        passive:
          default: false
          description: whether to send OSPF-Hello
          type: boolean
      type: object
    description: networks to participate in an OSPF area. The property key is the
      network name
    type: object
  type:
    default: default
    description: OSPF type, default (default) / stub / nssa
    enum:
    - default
    - stub
    - nssa
    type: string
title: junos_ospf_areas
type: object
x-examples:
  example-1:
    networks:
      '{network_name}':
        auth_keys:
          '{key}': string
        auth_password: string
        auth_type: none
        interface_type: nbma
        passive: true
    type: default
