description: Junos VRRP group
properties:
  auth_key:
    description: if `auth_type`==`md5`
    type: string
  auth_password:
    description: if `auth_type`==`simple`
    type: string
  auth_type:
    default: md5
    enum:
    - md5
    - simple
    type: string
  networks:
    additionalProperties:
      properties:
        ip:
          type: string
      type: object
    description: The property key is the network name
    type: object
title: junos_vrrp_group
type: object
x-examples:
  example-1:
    auth_key: string
    auth_password: string
    auth_type: none
    networks:
      '{network_name}':
        ip: string
