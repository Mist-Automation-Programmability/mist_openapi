content:
  application/json:
    schema:
      properties:
        end:
          type: integer
        limit:
          type: integer
        page:
          type: integer
        results:
          items:
            $ref: ../schemas/anomaly.yml
          minItems: 1
          type: array
          uniqueItems: true
        start:
          type: integer
      required:
      - start
      - end
      - limit
      - page
      - results
      type: object
description: OK
