content:
  application/json:
    examples:
      example-1:
        value:
          auto_upgrade:
            day_of_week: string
            enabled: true
            time_of_day: string
            version: string
          name: string
          vars:
            SSID_STR: string
            VLAN_ID: string
    schema:
      $ref: '../schemas/sitetemplate.yml'
description: Example response
