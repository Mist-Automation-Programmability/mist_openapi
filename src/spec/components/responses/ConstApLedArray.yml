content:
  application/json:
    examples:
      Example:
        value:
        - code: '01'
          description: LED not working
          key: LED_FAILURE
          name: LED Failure
    schema:
      items:
        $ref: '../schemas/const_ap_led.yml'
      type: array
description: List of AP Led Status
title: Constapledarray
