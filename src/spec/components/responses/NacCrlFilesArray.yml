content:
  application/json:
    schema:
      properties:
        results:
          items:
            $ref: '../schemas/nac_crl_file.yml'
          type: array
      type: object
description: Example response
