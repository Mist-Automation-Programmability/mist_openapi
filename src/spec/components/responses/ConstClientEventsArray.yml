content:
  application/json:
    examples:
      Example:
        value:
        - display: 11r Association
          key: CLIENT_AUTH_ASSOCIATION_11R
    schema:
      items:
        $ref: '../schemas/const_event.yml'
      type: array
description: List of Client Events definitions
