content:
  application/json:
    examples:
      example-1:
        value:
        - name: string
          paths:
            property1:
              bfd_profile: broadband
              ip: string
            property2:
              bfd_profile: lte
              ip: string
    schema:
      items:
        $ref: '../schemas/vpn.yml'
      type: array
description: Example response
