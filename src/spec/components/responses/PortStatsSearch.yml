content:
  application/json:
    schema:
      properties:
        end:
          type: integer
        limit:
          type: integer
        next:
          type: string
        results:
          items:
            $ref: '../schemas/switch_port_stats.yml'
          type: array
        start:
          type: integer
        total:
          type: integer
      required:
      - end
      - limit
      - results
      - start
      - total
      type: object
description: Example response
title: Portstatssearch
