content:
  application/json:
    examples:
      example-1:
        value:
          forward_url: https://my.sso/custom_logout_url
    schema:
      properties:
        forward_url:
          description: if configured in SSO as custom_logout_url
          type: string
      type: object
description: OK
title: Logout
