content:
  application/json:
    examples:
      Example:
        value:
          end: 1655151856
          results:
          - category: client
            reason: slow association
            recommendation: Ensure the IP helper-address is configured on the VLAN
              interface.
            text: Clients of the AP had slow association 8% of the time on Bhavabhi
              and 5 GHz. ...
          start: 1655065456
    schema:
      properties:
        end:
          example: 1655151856
          type: integer
        results:
          items:
            properties:
              category:
                example: client
                type: string
              reason:
                example: slow association
                type: string
              recommendation:
                example: Ensure the IP helper-address is configured on the VLAN interface.
                type: string
              text:
                example: Clients of the AP had slow association 8% of the time on
                  Bhavabhi and 5 GHz. ...
                type: string
            title: troubleshoot_item
            type: object
          type: array
        start:
          example: 1655065456
          type: integer
      type: object
description: Troubleshoot Response
title: Troubleshoot
