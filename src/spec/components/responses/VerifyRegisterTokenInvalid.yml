content:
  application/json:
    examples:
      not-found:
        value:
          detail: Not found.
    schema:
      properties:
        detail:
          type: string
      required:
      - detail
      type: object
description: Response if secret is invalid
title: Verifyregistertokeninvalid
