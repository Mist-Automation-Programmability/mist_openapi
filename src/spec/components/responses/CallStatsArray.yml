content:
  application/json:
    examples: {}
    schema:
      properties:
        end:
          type: number
        limit:
          type: integer
        next:
          type: string
        results:
          items:
            $ref: '../schemas/stats_call.yml'
          type: array
        start:
          type: number
        total:
          type: integer
      type: object
description: Example response
