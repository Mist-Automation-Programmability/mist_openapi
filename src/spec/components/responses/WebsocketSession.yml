content:
  application/json:
    examples:
      example-1:
        value:
          session: session_id
    schema:
      properties:
        session:
          type: string
      required:
      - session
      type: object
description: OK
title: Websocketsession
