content:
  application/json:
    examples:
      example-1:
        value:
        - addresses:
          - string
          apps:
          - string
          dscp_class: string
          hostnames:
          - string
          max_jitter: 0
          max_latency: string
          max_loss: 0
          name: string
          specs:
          - port_range: '0'
            protocol: any
          traffic_class: best_effort
          traffic_type: default
          type: custom
          vpn_name: addresses
    schema:
      $ref: '../schemas/service_list.yml'
description: OK
title: Servicesarray
