content:
  application/json:
    examples:
      Example:
        value:
        - alpha2: FR
          certified: true
          name: France
          numeric: 250
    schema:
      items:
        $ref: '../schemas/const_country.yml'
      type: array
description: List of Countries
