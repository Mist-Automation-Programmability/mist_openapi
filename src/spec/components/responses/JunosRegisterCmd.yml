content:
  application/json:
    examples:
      example-1:
        value:
          cmd: 'set system services ssh...

            ...

            set system services outbound-ssh client mist ...'
    schema:
      $ref: '../schemas/response_device_config_cmd.yml'
description: OK
title: Junosregistercmd
