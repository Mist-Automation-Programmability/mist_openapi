content:
  application/json:
    examples:
      Example:
        value:
          detail: Authentication credentials were not provided.
    schema:
      $ref: '../schemas/response_http401.yml'
description: Unauthorized
title: Http401
