content:
  application/json:
    examples:
      Example:
        value:
          detail: You do not have permission to perform this action.
    schema:
      $ref: '../schemas/response_http403.yml'
description: Permission Denied
title: Http403
