content:
  application/json:
    examples:
      invalid_email_address:
        value: {}
      new_email_address-already-exists:
        value:
          detail: email already existed
    schema:
      $ref: '../schemas/response_detail_string.yml'
description: invalid email address or new email address already exists
title: Updateselffailed
