content:
  application/json:
    examples:
      Example:
        value:
        - name: corporate only
          wlans:
          - auth:
              pairwise:
              - wpa1-tkip
              - wpa2-tkip
              type: psk
            band: both
            ssid: office
          - auth:
              type: open
            band: '5'
            ssid: office-guest
    schema:
      items:
        $ref: '../schemas/secpolicy.yml'
      type: array
description: OK
