content:
  application/json:
    examples:
      invalid-or-expired-token:
        value:
          detail: invalid token
      new_email_address-already-exists:
        value:
          detail: email already existed
      request-comes-from-authenticated_users-other-than-admin:
        value:
          detail: invalid token
    schema:
      $ref: '../schemas/response_detail_string.yml'
description: Bad Request
title: Selfverifyemailfailed
