content:
  application/json:
    examples:
      example-1:
        value:
          detail: sso admin login needs to be initiated by IdP
          forward_url: https://my.sso/idp_sso_url
    schema:
      $ref: '../schemas/response_login_failure.yml'
description: Login Failed
title: Loginfailure
