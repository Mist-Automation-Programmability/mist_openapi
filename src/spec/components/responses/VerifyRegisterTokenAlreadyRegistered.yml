content:
  application/json:
    examples:
      already-registered:
        value:
          detail: already registered
      verification-expired:
        value:
          detail: expired
    schema:
      $ref: '../schemas/response_detail_string.yml'
description: Response if verification expired or already registered
title: Verifyregistertokenalreadyregistered
