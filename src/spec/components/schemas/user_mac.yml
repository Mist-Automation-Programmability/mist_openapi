properties:
  labels:
    $ref: '../schemas/user-mac-labels.yml'
  mac:
    description: only non-local-admin MAC is accepted
    example: 5684dae9ac8b
    type: string
  notes:
    example: mac address refers to Canon printers
    type: string
  vlan:
    example: 30
    type: integer
title: User_Mac
type: object
