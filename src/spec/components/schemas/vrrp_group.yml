description: Junos VRRP group
properties:
  auth_key:
    description: if `auth_type`==`md5`
    example: auth-key-1
    type: string
  auth_password:
    description: if `auth_type`==`simple`
    type: string
  auth_type:
    $ref: '../schemas/vrrp_group_auth_type.yml'
  networks:
    $ref: '../schemas/vrrp_group_networks.yml'
title: Vrrp_Group
type: object
