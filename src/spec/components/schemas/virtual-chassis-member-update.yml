properties:
  mac:
    description: Required if `op`==`add` or `op`==`preprovision`.
    type: string
  member:
    description: Required if `op`==`remove` or `op`==`preprovision`. Optional if `op`==`add`
    type: integer
  vc_ports:
    $ref: '../schemas/virtual-chassis-member-update-vc-ports.yml'
  vc_role:
    $ref: '../schemas/virtual-chassis-member-update-vc-role.yml'
title: virtual_chassis_member_update
type: object
