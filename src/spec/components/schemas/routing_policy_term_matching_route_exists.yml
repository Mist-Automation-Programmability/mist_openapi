properties:
  route:
    example: 192.168.0.0/24
    type: string
  vrf_name:
    default: default
    description: 'name of the vrf instance

      it can also be the name of the VPN or wan if they'
    type: string
title: Routing_Policy_Term_Matching_Route_Exists
type: object
