properties:
  authentication_password:
    description: 'Not required if `authentication_type`==`authentication_none`

      include alphabetic, numeric, and special characters, but it cannot include control
      characters.'
    minLength: 7
    type: string
  authentication_type:
    $ref: '../schemas/snmp_usmp_user_authentication_type.yml'
  encryption_password:
    description: 'Not required if `encryption_type`==`privacy-none`

      include alphabetic, numeric, and special characters, but it cannot include control
      characters'
    minLength: 8
    type: string
  encryption_type:
    $ref: '../schemas/snmp_usmp_user_encryption_type.yml'
  name:
    type: string
title: Snmp_Usmp_User
type: object
