description: where this template should not be applied to (takes precedence)
properties:
  site_ids:
    $ref: '../schemas/template-exceptions-site-ids.yml'
  sitegroup_ids:
    $ref: '../schemas/template-exceptions-sitegroup-ids.yml'
title: template_exceptions
type: object
