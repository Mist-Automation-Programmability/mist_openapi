properties:
  input_networks_ingress:
    $ref: '../schemas/switch_port_mirroring_ingress_networks.yml'
  input_port_ids_egress:
    $ref: '../schemas/switch_port_mirroring_egress_port_ids.yml'
  input_port_ids_ingress:
    $ref: '../schemas/switch_port_mirroring_ingress_port_ids.yml'
  output_network:
    description: exaclty one of the `output_port_id` or `output_network` should be
      provided
    example: analyze
    type: string
  output_port_id:
    description: exaclty one of the `output_port_id` or `output_network` should be
      provided
    example: ge-0/0/5
    type: string
title: Switch_Port_Mirroring_Property
type: object
