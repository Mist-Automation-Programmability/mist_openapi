properties:
  egress_port_ids:
    description: at least one of `ingress_port_ids`,`egress_port_ids` or `ingress_networks`
      should be specified
    items:
      example: ge-0/0/3
      type: string
    type: array
  filter:
    type: object
  ingress_networks:
    description: at least one of `ingress_port_ids`,`egress_port_ids` or `ingress_networks`
      should be specified
    items:
      example: corp
      type: string
    type: array
  ingress_port_ids:
    description: at least one of `ingress_port_ids`,`egress_port_ids` or `ingress_networks`
      should be specified
    items:
      example: ge-0/0/3
      type: string
    type: array
  output_network:
    example: analyze
    type: string
  output_port_id:
    description: only one of of `output_port_id` or `output_network` should be specified
    example: ge-0/0/5
    type: string
title: Junos_Port_Mirror
type: object
