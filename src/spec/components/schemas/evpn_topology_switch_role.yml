description: 'use `role`==`none` to remove a switch from the topology. enum: `access`,
  `collapsed-core`, `core`, `distribution`, `esilag-access`, `none`'
enum:
- access
- collapsed-core
- core
- distribution
- esilag-access
- none
minLength: 1
title: Evpn_Topology_Switch_Role
type: string
