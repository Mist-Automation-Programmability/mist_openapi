properties:
  bands:
    $ref: '../schemas/utils-rrm-optimize-bands.yml'
  macs:
    $ref: '../schemas/utils-rrm-optimize-macs.yml'
  txpower_only:
    default: false
    description: only changng TX Power (will not disconnect clients)
    type: boolean
required:
- bands
title: Utils_Rrm_Optimize
type: object
