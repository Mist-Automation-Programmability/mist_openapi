properties:
  bands:
    $ref: '../schemas/utils_rrm_optimize_bands.yml'
  macs:
    $ref: '../schemas/utils_rrm_optimize_macs.yml'
  txpower_only:
    default: false
    description: only changng TX Power (will not disconnect clients)
    type: boolean
required:
- bands
title: Utils_Rrm_Optimize
type: object
