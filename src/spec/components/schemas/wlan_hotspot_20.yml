description: hostspot 2.0 wlan settings
properties:
  domain_name:
    $ref: '../schemas/wlan-hotspot20-domain-name.yml'
  enabled:
    description: whether to enable hotspot 2.0 config
    type: boolean
  nai_realms:
    $ref: '../schemas/strings.yml'
  operators:
    $ref: '../schemas/wlan-hotspot20-operators.yml'
  rcoi:
    $ref: '../schemas/wlan-hotspot20-rcoi.yml'
  venue_name:
    description: venue name, default is site name
    example: some_name
    type: string
title: Wlan_Hotspot_20
type: object
