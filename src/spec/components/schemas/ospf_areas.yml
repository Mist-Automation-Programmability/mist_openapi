description: Junos OSPF areas
properties:
  include_loopback:
    default: false
    type: boolean
  networks:
    additionalProperties:
      $ref: '../schemas/ospf_areas_network.yml'
    description: "Networks to participate in an OSPF area. \nProperty key is the network\
      \ name"
    example:
      corp:
        auth_keys:
          '1': auth-key-1
        auth_type: md5
        bfd_minimum_interval: 500
        dead_interval: 40
        hello_interval: 10
        interface_type: nbma
        metric: 10000
      guest:
        passive: true
    type: object
  type:
    $ref: '../schemas/ospf_areas_type.yml'
title: Ospf_Areas
type: object
