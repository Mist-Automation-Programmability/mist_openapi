properties:
  auth:
    $ref: '../schemas/ap_client_bridge_auth.yml'
  enabled:
    default: false
    description: "when acted as client bridge:\n  * only 5G radio can be used\n  *\
      \ will not serve as AP on any radios"
    type: boolean
  ssid:
    example: Uplink-SSID
    minLength: 1
    type: string
title: Ap_Client_Bridge
type: object
