default: link_down
description: 'Control when the DPC port should be changed to the default port usage

  Configuring to none will let the DPC port keep at the current port usage.'
enum:
- ''
- none
- link_down
example: link_down
title: Dynamic_Port_Usage_Reset_Default_When
type: string
