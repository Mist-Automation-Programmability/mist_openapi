properties:
  end:
    type: integer
  limit:
    type: integer
  next:
    description: the link to query next set of results. value is null if no next page
      exists.
    type: string
  results:
    items:
      $ref: '../schemas/rrm_event.yml'
    type: array
    uniqueItems: true
  start:
    type: integer
required:
- end
- limit
- results
- start
title: Response_Events_Rrm
type: object
