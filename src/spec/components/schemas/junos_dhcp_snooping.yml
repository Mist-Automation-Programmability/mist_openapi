properties:
  all_networks:
    type: boolean
  enable_arp_spoof_check:
    description: Enable for dynamic ARP inspection check
    type: boolean
  enable_ip_source_guard:
    description: Enable for check for forging source IP address
    type: boolean
  enabled:
    type: boolean
  networks:
    $ref: '../schemas/junos-dhcp-snooping-networks.yml'
title: Junos_Dhcp_Snooping
type: object
