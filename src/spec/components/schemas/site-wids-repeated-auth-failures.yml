properties:
  duration:
    description: window where a trigger will be detected and action to be taken (in
      seconds)
    example: 60
    type: number
  threshold:
    description: count of events to trigger
    type: integer
title: Site-Wids-Repeated-Auth-Failures
type: object
