description: 'if `scope`==`site`

  Object defines the scope (within the org e.g. whole org, and/or some site-groups,
  and/or some sites) for which the alarm service has to be suppressed for some `duration`'
properties:
  org_id:
    description: UUID of the current org (if provided, the alarms will be suppressed
      at org level)
    format: uuid
    type: string
  site_ids:
    $ref: '../schemas/suppressed-alarm-applies-site-ids.yml'
  sitegroup_ids:
    $ref: '../schemas/suppressed-alarm-applies-sitegroup-ids.yml'
title: Suppressed-Alarm-Applies
type: object
