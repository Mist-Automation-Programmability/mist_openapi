description: '`mode`==`dynamic` must only be used with the port usage with the name
  `dynamic`. enum: `access`, `dynamic`, `inet`, `trunk`'
enum:
- access
- dynamic
- inet
- trunk
title: Switch_Port_Usage_Mode
type: string
