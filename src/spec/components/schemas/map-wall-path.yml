description: a JSON blob for wall definition (same format as wayfinding_path)
properties:
  coordinate:
    example: actual
    type: string
  nodes:
    $ref: '../schemas/map-wall-path-nodes.yml'
title: Map-Wall-Path
type: object
