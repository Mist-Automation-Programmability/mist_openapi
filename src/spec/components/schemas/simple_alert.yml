description: 'Set of heuristic rules will be enabled when marvis subscription is not
  available.

  It triggers when, in a Z minute window, there are more than Y distinct client encountring
  over X failures'
properties:
  arp_failure:
    $ref: '../schemas/simple-alert-arp-failure.yml'
  dhcp_failure:
    $ref: '../schemas/simple-alert-dhcp-failure.yml'
  dns_failure:
    $ref: '../schemas/simple-alert-dns-failure.yml'
title: Simple_Alert
type: object
