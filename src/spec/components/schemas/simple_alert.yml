description: 'Set of heuristic rules will be enabled when marvis subscription is not
  available.

  It triggers when, in a Z minute window, there are more than Y distinct client encountring
  over X failures'
properties:
  arp_failure:
    $ref: '../schemas/simple_alert_arp_failure.yml'
  dhcp_failure:
    $ref: '../schemas/simple_alert_dhcp_failure.yml'
  dns_failure:
    $ref: '../schemas/simple_alert_dns_failure.yml'
title: Simple_Alert
type: object
