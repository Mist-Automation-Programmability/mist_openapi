default: model
description: 'enum: `ap`, `apfw`, `model`, `org_id`, `site_id`, `text`, `timestamp`,
  `type`'
enum:
- ap
- apfw
- model
- org_id
- site_id
- text
- timestamp
- type
title: Org_Devices_Events_Count_Distinct
type: string
