properties:
  no_reveal:
    default: false
    description: 'by default, API hides password/secrets when the user doesn''t have
      write access

      * `true`: API will hide passwords/secrets for all users

      * `false`: API will hide passwords/secrests for read-only users'
    type: boolean
title: Org_Setting_Api_Policy
type: object
