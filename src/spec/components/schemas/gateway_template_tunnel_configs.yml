properties:
  auto_provision:
    $ref: '../schemas/gateway-template-tunnel-configs-auto-provision.yml'
  ike_lifetime:
    description: 'Only if:

      * `provider`== `custom-ipsec`'
    type: integer
  ike_mode:
    $ref: '../schemas/gateway_template_tunnel_ike_mode.yml'
  ike_proposals:
    $ref: '../schemas/gateway-template-tunnel-configs-ike-proposals.yml'
  ipsec_lifetime:
    description: if `provider`== `custom-ipsec`
    type: integer
  ipsec_proposals:
    $ref: '../schemas/gateway-template-tunnel-configs-ipsec-proposals.yml'
  local_id:
    description: 'Only if:

      * `provider`== `zscaler-ipsec`

      * `provider`==`jse-ipsec`

      * `provider`== `custom-ipsec`'
    type: string
  mode:
    $ref: '../schemas/gateway_template_tunnel_mode.yml'
  primary:
    $ref: '../schemas/gateway_template_tunnel_node.yml'
  probe:
    $ref: '../schemas/gateway_template_tunnel_probe.yml'
  protocol:
    $ref: '../schemas/gateway_template_tunnel_protocol.yml'
  provider:
    $ref: '../schemas/gateway_template_tunnel_provider_name.yml'
  psk:
    description: 'Only if:

      * `provider`== `zscaler-ipsec`

      * `provider`==`jse-ipsec`

      * `provider`== `custom-ipsec`'
    type: string
  secondary:
    $ref: '../schemas/gateway_template_tunnel_node.yml'
  version:
    $ref: '../schemas/gateway_template_tunnel_version.yml'
title: Gateway_Template_Tunnel_Configs
type: object
