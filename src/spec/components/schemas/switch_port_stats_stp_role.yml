description: 'if `up`==`true`. enum: `alternate`, `backup`, `designated`, `root`,
  `root-prevented`'
enum:
- alternate
- backup
- designated
- root
- root-prevented
title: Switch_Port_Stats_Stp_Role
type: string
