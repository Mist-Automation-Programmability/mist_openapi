properties:
  dscp:
    type: integer
  dst_subnet:
    description: subnet filter is not required but helps AP to only inspect certain
      traffic (thus reducing AP load)
    type: string
  src_subnet:
    description: subnet filter is not required but helps AP to only inspect certain
      traffic (thus reducing AP load)
    type: string
title: Wlan_App_Qos_Apps_Properties
type: object
