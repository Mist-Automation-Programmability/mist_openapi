properties:
  hostname:
    description: if `type`==`dns`
    type: string
  ip:
    description: if `type`==`arp`
    type: string
  password:
    description: if `type`==`radius`
    type: string
  port_id:
    description: if `type`==`ssr`
    type: string
  type:
    $ref: '../schemas/synthetic_test_type.yml'
  url:
    description: if `type`==`curl`
    type: string
  username:
    description: if `type`==`radius`
    type: string
  vlan_id:
    description: required for AP
    type: integer
required:
- type
title: Synthetic_Test_Device
type: object
