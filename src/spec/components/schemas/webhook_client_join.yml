description: client join webhook sample
properties:
  events:
    $ref: '../schemas/webhook-client-join-events.yml'
  topic:
    default: client-join
    type: string
required:
- events
- topic
title: Webhook_Client_Join
type: object
