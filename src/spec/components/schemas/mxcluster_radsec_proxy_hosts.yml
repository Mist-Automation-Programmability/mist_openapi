description: hostnames or IPs for Mist AP to use as the TLS Server (i.e. they are
  reachable from AP) in addition to `tunterm_hosts`
items:
  type: string
title: Mxcluster_Radsec_Proxy_Hosts
type: array
