description: SDK Client Details statistics
properties:
  id:
    format: uuid
    type: string
  last_seen:
    description: last seen timestamp
    type: number
  map_id:
    description: map_id of the sdk client (if known), or null
    format: uuid
    nullable: true
    type: string
  name:
    description: name of the sdk client (if provided)
    type: string
  network_connection:
    $ref: '../schemas/sdkclient_stats_network_connection.yml'
  uuid:
    description: uuid of the sdk client
    format: uuid
    type: string
  vbeacons:
    $ref: '../schemas/sdkclient-wireless-stats-vbeacons.yml'
  x:
    description: x (in pixels) of user location on the map (if known)
    type: number
  y:
    description: y (in pixels) of user location on the map (if known)
    type: number
  zones:
    $ref: '../schemas/sdkclient-wireless-stats-zone.yml'
required:
- id
- uuid
- last_seen
title: Sdkstats_Wireless_Client
type: object
