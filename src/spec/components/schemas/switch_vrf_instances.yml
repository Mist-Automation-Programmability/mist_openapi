additionalProperties:
  $ref: '../schemas/switch_vrf_instance.yml'
description: Property key is the network name
example:
  guest:
    extra_routes:
      0.0.0.0/0:
        via: 192.168.31.1
    networks:
    - guest
title: Switch_Vrf_Instances
type: object
