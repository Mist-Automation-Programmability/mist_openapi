properties:
  ports:
    description: a list of ports on which to clear mac addresses. must include logical
      unit number
    items:
      type: string
    type: array
title: Utils_Clear_Macs
type: object
