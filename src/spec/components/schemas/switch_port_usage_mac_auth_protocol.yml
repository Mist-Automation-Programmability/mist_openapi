default: eap-md5
description: Only if `mode`!=`dynamic` and `enable_mac_auth` ==`true`. This type is
  ignored if mist_nac is enabled.
enum:
- pap
- eap-peap
- eap-md5
title: Switch_Port_Usage_Mac_Auth_Protocol
type: string
