properties:
  node:
    $ref: '../schemas/ha_cluster_node_enum.yml'
  prefix:
    description: IP Prefix
    example: 3.1.1.0/24
    type: string
  service_ip:
    description: only supported with SSR
    example: 3.1.1.10
    type: string
  service_name:
    description: only supported with SSR
    example: internet-wan_and_lte
    type: string
  service_port:
    description: only supported with SSR
    example: 32768
    type: integer
  service_protocol:
    description: only supported with SSR
    example: udp
    type: string
  service_tenant:
    description: only supported with SSR
    example: branch1-wifi-mgt
    type: string
  vrf:
    description: VRF Name
    example: guest
    type: string
title: Utils_Show_Forwarding_Table
type: object
