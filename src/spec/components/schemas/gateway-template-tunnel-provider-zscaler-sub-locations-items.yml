properties:
  aup_acceptance_required:
    default: true
    type: boolean
  aup_expire:
    default: 1
    description: days before AUP is requested again
    type: integer
  aup_ssl_proxy:
    default: false
    description: proxy HTTPs traffic, requiring Zscaler cert to be installed in browser
    type: boolean
  download_mbps:
    description: the download bandwidth cap of the link, in Mbps
    example: 200
    type: integer
  enable_aup:
    description: if `use_xff`==`true`, display Acceptable Use Policy (AUP)
    type: boolean
  enable_caution:
    default: false
    description: when `enforce_authentication`==`false`, display caution notification
      for non-authenticated users
    type: boolean
  enforce_authentication:
    default: false
    type: boolean
  subnets:
    $ref: '../schemas/gateway-template-tunnel-provider-zscaler-sub-locations-items-subnets.yml'
  upload_mbps:
    description: the download bandwidth cap of the link, in Mbps
    example: 200
    type: integer
title: Gateway-Template-Tunnel-Provider-Zscaler-Sub-Locations-Items
type: object
