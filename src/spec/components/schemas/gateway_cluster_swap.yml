properties:
  mac:
    description: "when `op`\_==`replacement_nodeX`, new node1\n's MAC, the device\
      \ has to be standalone and assigned to the same site"
    type: string
  op:
    default: swap
    enum:
    - swap
    - replace_node1
    minLength: 1
    title: gateway_cluster_swap_operation
    type: string
required:
- op
title: Gateway_Cluster_Swap
type: object
