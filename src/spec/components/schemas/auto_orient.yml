properties:
  force_collection:
    default: false
    description: "If `force_collection`==`false`, the API attempts to start auto orientation\
      \ with existing BLE data. \nIf `force_collection`==`true`, the API attempts\
      \ to start BLE orchestration."
    type: boolean
  macs:
    description: list of device macs
    items:
      type: string
    type: array
title: Auto_Orient
type: object
