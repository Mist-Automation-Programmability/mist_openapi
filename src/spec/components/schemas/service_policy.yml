properties:
  action:
    $ref: '../schemas/allow_deny.yml'
  appqoe:
    $ref: '../schemas/service-policy-appqoe.yml'
  ewf:
    $ref: '../schemas/service-policy-ewf.yml'
  idp:
    $ref: '../schemas/idp_config.yml'
  local_routing:
    default: false
    description: access within the same VRF
    type: boolean
  name:
    type: string
  path_preferences:
    description: 'by default, we derive all paths available and use them

      optionally, you can customize by using `path_preference`'
    type: string
  servicepolicy_id:
    description: used to link servicepolicy defined at org level and overwrite some
      attributes
    format: uuid
    type: string
  services:
    $ref: '../schemas/strings.yml'
  tenants:
    $ref: '../schemas/strings.yml'
title: Service_Policy
type: object
