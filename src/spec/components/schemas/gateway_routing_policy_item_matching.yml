description: zero or more criteria/filter can be specified to match the term, all
  criteria have to be met
properties:
  as_path:
    $ref: '../schemas/gateway-routing-policy-item-matching-as-path.yml'
  community:
    $ref: '../schemas/gateway-routing-policy-item-matching-community.yml'
  network:
    $ref: '../schemas/strings.yml'
  prefix:
    $ref: '../schemas/gateway-routing-policy-item-matching-prefix.yml'
  protocol:
    $ref: '../schemas/gateway-routing-policy-item-matching-protocol.yml'
  route_exists:
    $ref: '../schemas/gateway_routing_policy_item_matching_route_exists.yml'
  vpn_neighbor_mac:
    $ref: '../schemas/gateway-routing-policy-item-matching-vpn-neighbor-mac.yml'
  vpn_path:
    $ref: '../schemas/gateway-routing-policy-item-matching-vpn-path.yml'
  vpn_path_sla:
    $ref: '../schemas/gateway_routing_policy_item_matching_vpn_path_sla.yml'
title: Gateway_Routing_Policy_Item_Matching
type: object
