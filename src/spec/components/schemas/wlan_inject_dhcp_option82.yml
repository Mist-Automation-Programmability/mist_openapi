properties:
  circuit_id:
    example: '{{SSID}}:{{AP_MAC}}'
    type: string
  enabled:
    default: false
    description: whether to inject option 82 when forwarding DHCP packets
    type: boolean
title: Wlan_Inject_Dhcp_Option82
type: object
