description: list of RADIUS authentication servers, at least one is needed if `auth
  type`==`eap`, order matters where the first one is treated as primary
items:
  $ref: '../schemas/radius_auth_server.yml'
title: Wlan_Auth_Servers
type: array
