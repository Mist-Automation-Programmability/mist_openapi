description: 'if `up`==`true` and has Authenticator role. enum: `authenticated`, `authenticating`,
  `held`, `init`'
enum:
- authenticated
- authenticating
- held
- init
title: Switch_Port_Stats_Auth_State
type: string
