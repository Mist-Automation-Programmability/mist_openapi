properties:
  egress_port_ids:
    $ref: '../schemas/junos-port-mirroring-egress-port-ids.yml'
  ingress_networks:
    $ref: '../schemas/junos-port-mirroring-ingress-networks.yml'
  ingress_port_ids:
    $ref: '../schemas/junos-port-mirroring-ingress-port-ids.yml'
  output_network:
    example: analyze
    type: string
  output_port_id:
    description: exaclty on of the `output_port_id` or `output_network` should be
      provided
    example: ge-0/0/5
    type: string
title: Junos_Port_Mirroring
type: object
