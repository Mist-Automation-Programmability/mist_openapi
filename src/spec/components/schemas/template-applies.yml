description: where this template should be applied to, can be org_id, site_ids, sitegroup_ids
properties:
  org_id:
    $ref: '../schemas/org_id.yml'
  site_ids:
    $ref: '../schemas/template-applies-site-ids.yml'
  sitegroup_ids:
    $ref: '../schemas/template-applies-sitegroup-ids.yml'
title: template_applies
type: object
