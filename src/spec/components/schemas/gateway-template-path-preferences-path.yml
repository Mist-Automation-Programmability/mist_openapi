properties:
  cost:
    type: integer
  disabled:
    description: For SSR Only. `true`` if this specific path is undesired
    type: boolean
  gateway_ip:
    description: if `type`==`local`, if a different gateway is desired
    type: string
  internet_access:
    default: false
    description: when `type`==`vpn`, if this vpn path can be used for internet
    type: boolean
  name:
    type: string
  networks:
    $ref: '../schemas/gateway-template-path-preferences-path-networks.yml'
  target_ips:
    $ref: '../schemas/gateway-template-path-preferences-path-target-ips.yml'
  type:
    $ref: '../schemas/gateway-path-type.yml'
  wan_name:
    description: Spoke's outgoing wan
    example: wan0
    type: string
title: gateway_template_path_preferences_path
type: object
