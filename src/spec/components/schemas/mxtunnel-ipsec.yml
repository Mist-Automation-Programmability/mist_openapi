properties:
  dns_servers:
    $ref: '../schemas/mxtunnel-ipsec-dns-servers.yml'
  dns_suffix:
    $ref: '../schemas/strings.yml'
  enabled:
    type: boolean
  extra_routes:
    $ref: '../schemas/mxtunnel-ipsec-extra-routes.yml'
  split_tunnel:
    type: boolean
  use_mxedge:
    type: boolean
title: mxtunnel_ipsec
type: object
