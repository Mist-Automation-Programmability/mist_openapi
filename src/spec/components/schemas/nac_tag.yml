properties:
  allow_usermac_override:
    default: false
    description: can be set to true to allow the override by usermac result
    type: boolean
  created_time:
    readOnly: true
    type: number
  egress_vlan_names:
    $ref: '../schemas/nac_tag_egress_vlan_names.yml'
  gbp_tag:
    description: if `type`==`gbp_tag`
    type: integer
  id:
    format: uuid
    readOnly: true
    type: string
  match:
    $ref: '../schemas/nac_tag_match.yml'
  match_all:
    default: false
    description: 'This field is applicable only when `type`==`match`

      * `false`: means it is sufficient to match any of the values (i.e., match-any
      behavior)

      * `true`: means all values should be matched (i.e., match-all behavior)



      Currently it makes sense to set this field to `true` only if the `match`==`idp_role`
      or `match`==`usermac_label`'
    type: boolean
  modified_time:
    readOnly: true
    type: number
  name:
    minLength: 1
    type: string
  org_id:
    $ref: '../schemas/org_id.yml'
  radius_attrs:
    $ref: '../schemas/nac_tag_radius_attrs.yml'
  radius_group:
    description: if `type`==`radius_group`
    type: string
  radius_vendor_attrs:
    $ref: '../schemas/nac_tag_radius_vendor_attrs.yml'
  session_timeout:
    description: if `type`==`session_timeout, in seconds
    example: 86000
    type: integer
  type:
    $ref: '../schemas/nac_tag_type.yml'
  values:
    $ref: '../schemas/nac_tag_values.yml'
  vlan:
    description: if `type`==`vlan`
    type: string
required:
- name
- type
title: Nac_Tag
type: object
