description: "enum:\n  * `upgrade_to_mist`: Upgrade to mist-managed\n  * `downgrade_to_jsi`:\
  \ Downgrade to basic monitoring. When downgrading a VC member to jsi, we will move\
  \ the cloud connection of the VC to jsi-terminator and keep all VC device/inventories\
  \ intact for pain-free upgrading back to mist.\n  * `assign`: Assign inventory to\
  \ a site\n  * `unassign`: Unassign inventory from a site\n  * `delete`: Delete multiple\
  \ inventory from org. If the device is already assigned to a site, it will be unassigned"
enum:
- assign
- delete
- downgrade_to_jsi
- unassign
- upgrade_to_mist
title: Inventory_Update_Operation
type: string
