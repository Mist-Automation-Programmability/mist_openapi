description: Default VLAN ID(s) can be a number, a range of VLAN IDs, a variable or
  multiple numbers, ranges or variables as a VLAN pool. Default VLAN as a pool of
  VLANS requires 0.14.x or newer firmware
items:
  $ref: '../schemas/wlan_dynamic_vlan_default_vlan_id.yml'
title: Wlan_Dynamic_Vlan_Default_Vlan_Ids
type: array
