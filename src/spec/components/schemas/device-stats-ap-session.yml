properties:
  local_sid:
    description: remote sessions id (dynamically unless Tunnel is said to be static)
    example: 31
    type: integer
  remote_id:
    description: WxlanTunnel Remote ID (user-configured)
    example: vpn1
    type: string
  remote_sid:
    description: remote sessions id (dynamically unless Tunnel is said to be static)
    example: 13
    type: integer
  state:
    $ref: '../schemas/l2tp_state.yml'
title: device_stats_ap_session
type: object
