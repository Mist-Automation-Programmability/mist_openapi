description: when used as import policy
properties:
  accept:
    type: boolean
  add_community:
    $ref: '../schemas/gateway-routing-policy-item-action-add-community.yml'
  add_target_vrfs:
    $ref: '../schemas/gateway-routing-policy-item-action-add-target-vrfs.yml'
  community:
    $ref: '../schemas/gateway-routing-policy-item-action-community.yml'
  exclude_as_path:
    $ref: '../schemas/gateway-routing-policy-item-action-exclude-as-path.yml'
  exclude_community:
    $ref: '../schemas/gateway-routing-policy-item-action-exclude-community.yml'
  export_communitites:
    $ref: '../schemas/gateway-routing-policy-item-action-export-communitites.yml'
  local_preference:
    description: optional, for an import policy, local_preference can be changed
    type: string
  prepend_as_path:
    $ref: '../schemas/gateway-routing-policy-item-action-prepend-as-path.yml'
title: Gateway_Routing_Policy_Item_Action
type: object
