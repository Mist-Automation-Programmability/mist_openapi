description: when used as import policy
properties:
  accept:
    type: boolean
  add_community:
    items:
      example: '65003:10'
      type: string
    type: array
  add_target_vrfs:
    description: for SSR, hub decides how VRF routes are leaked on spoke
    items:
      type: string
    type: array
  community:
    description: when used as export policy, optional
    items:
      example: '65003:10'
      type: string
    type: array
  exclude_as_path:
    description: when used as export policy, optional. To exclude certain AS
    items:
      example: '65002'
      type: string
    type: array
  exclude_community:
    items:
      example: '65003:10'
      type: string
    type: array
  export_communitites:
    description: when used as export policy, optional
    items:
      type: string
    type: array
  local_preference:
    description: optional, for an import policy, local_preference can be changed
    type: string
  prepend_as_path:
    description: when used as export policy, optional. By default, the local AS will
      be prepended, to change it
    items:
      example: '65003'
      type: string
    type: array
title: Gateway_Routing_Policy_Item_Action
type: object
