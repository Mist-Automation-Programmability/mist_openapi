description: Initiate a packet Capture for New Wireless Client Associations
properties:
  ap_mac:
    example: a83a79a947ee
    type: string
  client_mac:
    description: client mac, required if `type`==`client`; optional otherwise
    example: 60a10a773412
    type: string
  duration:
    default: 600
    description: duration of the capture, in seconds
    example: 600
    maximum: 86400
    type: integer
  includes_mcast:
    default: false
    type: boolean
  max_pkt_len:
    default: 128
    example: 128
    maximum: 2048
    type: integer
  num_packets:
    default: 100
    description: number of packets to capture, 0 for unlimited
    example: 100
    type: integer
  ssid:
    description: optional filter by ssid
    example: null
    type: string
  type:
    $ref: '../schemas/capture_new_assoc_type.yml'
required:
- type
title: Capture_New_Assoc
type: object
