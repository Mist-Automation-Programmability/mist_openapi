description: 'enum: `any`, `dynamic_gbp`, `mac`, `network`, `radius_group`, `resource`,
  `static_gbp`, `subnet`'
enum:
- any
- dynamic_gbp
- mac
- network
- radius_group
- resource
- static_gbp
- subnet
title: Acl_Tag_Type
type: string
