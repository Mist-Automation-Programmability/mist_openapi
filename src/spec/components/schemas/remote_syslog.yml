properties:
  archive:
    $ref: '../schemas/remote-syslog-archive.yml'
  console:
    $ref: '../schemas/remote-syslog-console.yml'
  enabled:
    default: false
    type: boolean
  files:
    $ref: '../schemas/remote-syslog-files.yml'
  network:
    description: if source_address is configured, will use the vlan firstly otherwise
      use source_ip
    example: default
    type: string
  send_to_all_servers:
    default: false
    type: boolean
  servers:
    $ref: '../schemas/remote-syslog-servers.yml'
  time_format:
    $ref: '../schemas/remote-syslog-time-format.yml'
  users:
    $ref: '../schemas/remote-syslog-users.yml'
title: Remote_Syslog
type: object
