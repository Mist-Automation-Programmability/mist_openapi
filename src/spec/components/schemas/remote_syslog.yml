properties:
  archive:
    $ref: '../schemas/remote_syslog_archive.yml'
  console:
    $ref: '../schemas/remote_syslog_console.yml'
  enabled:
    default: false
    type: boolean
  files:
    $ref: '../schemas/remote_syslog_files.yml'
  network:
    description: if source_address is configured, will use the vlan firstly otherwise
      use source_ip
    example: default
    type: string
  send_to_all_servers:
    default: false
    type: boolean
  servers:
    $ref: '../schemas/remote_syslog_servers.yml'
  time_format:
    $ref: '../schemas/remote_syslog_time_format.yml'
  users:
    $ref: '../schemas/remote_syslog_users.yml'
title: Remote_Syslog
type: object
