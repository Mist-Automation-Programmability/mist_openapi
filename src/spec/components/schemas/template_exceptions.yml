description: where this template should not be applied to (takes precedence)
properties:
  site_ids:
    $ref: '../schemas/template_exceptions_site_ids.yml'
  sitegroup_ids:
    $ref: '../schemas/template_exceptions_sitegroup_ids.yml'
title: Template_Exceptions
type: object
