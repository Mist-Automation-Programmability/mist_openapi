properties:
  config_revert_timer:
    default: 10
    description: rollback timer for commit confirmed
    maximum: 30
    minimum: 1
    type: integer
  protect_re:
    $ref: '../schemas/protect_re.yml'
title: Switch_Switch_Mgmt
type: object
