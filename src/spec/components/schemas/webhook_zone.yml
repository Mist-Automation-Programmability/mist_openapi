description: zone webhook sample
properties:
  events:
    $ref: '../schemas/webhook_zone_events.yml'
  topic:
    default: zone
    description: topic subscribed to
    type: string
required:
- events
- topic
title: Webhook_Zone
type: object
