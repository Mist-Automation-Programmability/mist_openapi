properties:
  category:
    example: client
    type: string
  reason:
    example: slow association
    type: string
  recommendation:
    example: Ensure the IP helper-address is configured on the VLAN interface.
    type: string
  text:
    example: Clients of the AP had slow association 8% of the time on Bhavabhi and
      5 GHz. ...
    type: string
title: Response_Troubleshoot_Item
type: object
