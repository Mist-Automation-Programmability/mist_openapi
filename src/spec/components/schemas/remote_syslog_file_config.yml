properties:
  archive:
    $ref: '../schemas/remote_syslog_archive.yml'
  contents:
    $ref: '../schemas/remote_syslog_contents.yml'
  explicit_priority:
    type: boolean
  file:
    example: file-name
    type: string
  match:
    example: '!alarm|ntp|errors.crc_error[chan]'
    type: string
  structured_data:
    type: boolean
title: Remote_Syslog_File_Config
type: object
