description: 'if `up`==`true`. enum: `blocking`, `disabled`, `forwarding`, `learning`,
  `listening`'
enum:
- blocking
- disabled
- forwarding
- learning
- listening
title: Switch_Port_Stats_Stp_State
type: string
