properties:
  hosts:
    items:
      description: IP Address of the remote host
      type: string
    type: array
  internal_ips:
    description: "Only if:\n* `provider`== `zscaler-gre` \n* `provider`== `custom-gre`"
    items:
      type: string
    type: array
  probe_ips:
    $ref: '../schemas/string_list.yml'
  remote_ids:
    description: 'Only if:

      * `provider`== `custom-ipsec`'
    items:
      type: string
    type: array
  wan_names:
    items:
      type: string
    type: array
title: Gateway_Template_Tunnel_Node
type: object
