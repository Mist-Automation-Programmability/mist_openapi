properties:
  auto_upgrade:
    properties:
      day_of_week:
        $ref: '../schemas/day_of_week.yml'
      enabled:
        type: boolean
      time_of_day:
        type: string
      version:
        type: string
    type: object
  name:
    type: string
  vars:
    $ref: '../schemas/vars.yml'
title: Site_Template
type: object
