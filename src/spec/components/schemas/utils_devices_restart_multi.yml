properties:
  device_ids:
    $ref: '../schemas/utils-devices-restart-multi-device-ids.yml'
  node:
    description: 'only for SSR: if node is not present, both nodes are restarted

      for other devices: node should not be present'
    type: string
required:
- device_ids
title: Utils_Devices_Restart_Multi
type: object
