default: shuffle
description: "Ordering of tunterm_hosts for mxedge within the same mxcluster. enum:\n\
  \  * `shuffle`: the ordering of tunterm_hosts is randomized by the device''s MAC\n\
  \  * `shuffle-by-site`: shuffle by site_id+tunnel_id (so when client connects to\
  \ a specific Tunnel, it will go to the same (order of) mxedge, and we load-balancing\
  \ between tunnels)\n  * `ordered`: order decided by tunterm_hosts_order"
enum:
- ordered
- shuffle
- shuffle-by-site
title: Mxcluster_Tunterm_Hosts_Selection
type: string
