default: shuffle
description: "Ordering of tunterm_hosts for mxedge within the same mxcluster.\n  *\
  \ When `shuffle`, the ordering of tunterm_hosts is randomized by the device\\u2019\\\
  s MAC.\n  * When `shuffle-by-site`, we shuffle by site_id+tunnel_id (so when client\
  \ connects to a specific Tunnel, it will go to the same (order of) mxedge, and we\
  \ load-balancing between tunnels).\n  * When `ordered`, the order is decided by\
  \ tunterm_hosts_order"
enum:
- shuffle
- shuffle-by-site
- ordered
title: Mxcluster_Tunterm_Hosts_Selection
type: string
