properties:
  end:
    type: integer
  limit:
    type: integer
  next:
    description: the link to query next set of results. value is null if no next page
      exists.
    type: string
  results:
    $ref: '../schemas/response_events_fastroam_results.yml'
  start:
    type: integer
required:
- start
- end
- limit
- results
title: Response_Events_Fastroam
type: object
