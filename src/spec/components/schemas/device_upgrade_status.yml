description: 'status upgrade is in. enum: `cancelled`, `completed`, `created`, `downloaded`,
  `downloading`, `failed`, `upgrading`'
enum:
- cancelled
- completed
- created
- downloaded
- downloading
- failed
- upgrading
title: Device_Upgrade_Status
type: string
