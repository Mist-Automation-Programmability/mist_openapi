properties:
  authentication_password:
    description: 'Not required if `authentication_type`==`authentication-none`

      include alphabetic, numeric, and special characters, but it cannot include control
      characters.'
    minLength: 7
    type: string
  authentication_type:
    $ref: '../schemas/snmp-usmp-user-authentication-type.yml'
  encryption_password:
    description: 'Not required if `encryption_type`==`privacy-none`

      include alphabetic, numeric, and special characters, but it cannot include control
      characters'
    minLength: 8
    type: string
  encryption_type:
    $ref: '../schemas/snmp-usmp-user-encryption-type.yml'
  name:
    type: string
title: snmp_usmp_user
type: object
