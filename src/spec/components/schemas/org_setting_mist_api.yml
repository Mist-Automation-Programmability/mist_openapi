properties:
  secret_policy:
    default: show
    description: by default, API hides password/secrets when the user doesn't have
      write access it can be more strictly controlled
    enum:
    - show
    - strict
    type: string
title: Org_Setting_Mist_Api
type: object
