description: Virtual Chassis
properties:
  member:
    description: Only if `op`==`renumber`
    type: integer
  members:
    $ref: '../schemas/virtual_chassis_update_members.yml'
  new-member:
    description: Only if `op`==`renumber`
    type: integer
  op:
    $ref: '../schemas/virtual_chassis_update_op.yml'
title: Virtual_Chassis_Update
type: object
