description: Virtual Chassis
properties:
  member:
    description: Only if `op`==`renumber`
    type: integer
  members:
    $ref: '../schemas/virtual-chassis-update-members.yml'
  new-member:
    description: Only if `op`==`renumber`
    type: integer
  op:
    $ref: '../schemas/virtual-chassis-update-op.yml'
title: Virtual_Chassis_Update
type: object
