properties:
  bfd_profile:
    $ref: '../schemas/gateway_port_vpn_path_bfd_profile.yml'
  bfd_use_tunnel_mode:
    default: false
    description: whether to use tunnel mode. SSR only
    type: boolean
  preference:
    description: for a given VPN, when `path_selection.strategy`==`simple`, the preference
      for a path (lower is preferred)
    type: integer
  role:
    $ref: '../schemas/gateway_port_vpn_path_role.yml'
  traffic_shaping:
    $ref: '../schemas/gateway_traffic_shaping.yml'
title: Gateway_Port_Vpn_Path
type: object
