description: 'enum: `blocking`, `disabled`, `forwarding`, `learning`, `listening`'
enum:
- blocking
- disabled
- forwarding
- learning
- listening
title: Search_Site_Switch_Ports_Stp_State
type: string
