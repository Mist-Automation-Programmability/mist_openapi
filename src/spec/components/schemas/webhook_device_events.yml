description: device event webhook sample
properties:
  events:
    $ref: '../schemas/webhook-device-events-events.yml'
  topic:
    default: device-events
    description: topic subscribed to
    type: string
required:
- events
- topic
title: Webhook_Device_Events
type: object
