description: device event webhook sample
properties:
  events:
    $ref: '../schemas/webhook_device_events_events.yml'
  topic:
    default: device_events
    description: topic subscribed to
    type: string
required:
- events
- topic
title: Webhook_Device_Events
type: object
