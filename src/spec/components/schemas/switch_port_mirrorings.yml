additionalProperties:
  $ref: '../schemas/switch_port_mirroring.yml'
description: 'Property key is the port mirroring instance name

  port_mirroring can be added under device/site settings. It takes interface and ports
  as input for ingress, interface as input for egress and can take interface and port
  as output.'
title: Switch_Port_Mirrorings
type: object
