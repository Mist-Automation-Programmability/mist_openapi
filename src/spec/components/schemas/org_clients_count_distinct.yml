default: device
description: 'enum: `ap`, `device`, `hostname`, `ip`, `mac`, `model`, `os`, `ssid`,
  `vlan`'
enum:
- ap
- device
- hostname
- ip
- mac
- model
- os
- ssid
- vlan
title: Org_Clients_Count_Distinct
type: string
