default: sequential
description: "sequential or parallel (default parallel). Whether to upgrade mesh AP\u2019\
  s parallelly or sequentially at the end of the upgrade"
enum:
- ''
- sequential
- parallel
title: Device_Upgrade_Rrm_Mesh_Upgrade
type: string
