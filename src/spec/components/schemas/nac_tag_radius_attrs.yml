description: "if `type`==`radius_attrs`, user can specify a list of one or more standard\
  \ attributes in the field \"radius_attrs\". \nIt is the responsibility of the user\
  \ to provide a syntactically correct string, otherwise it may not work as expected.\n\
  Note that it is allowed to have more than one radius_attrs in the result of a given\
  \ rule."
example:
- Idle-Timeout=600
- Termination-Action=RADIUS-Request
items:
  type: string
title: Nac_Tag_Radius_Attrs
type: array
