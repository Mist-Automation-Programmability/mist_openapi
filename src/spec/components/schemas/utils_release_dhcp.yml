properties:
  node:
    $ref: '../schemas/ha_cluster_node.yml'
  port:
    description: The nework interface on which to release the current DHCP release
    example: wan-interface
    minLength: 1
    type: string
required:
- port
title: Utils_Release_Dhcp
type: object
