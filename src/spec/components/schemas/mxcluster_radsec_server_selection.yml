default: ordered
description: ordered (default) / unordered. When ordered, Mist Edge will prefer and
  go back to the first radius server if possible
enum:
- ordered
- unordered
title: Mxcluster_Radsec_Server_Selection
type: string
