default: policy
description: 'enum: `mac`, `path_type`, `peer_mac`, `peer_port_id`, `policy`, `port_id`,
  `tenant`'
enum:
- mac
- path_type
- peer_mac
- peer_port_id
- policy
- port_id
- tenant
title: Wan_Usages_Count_Disctinct
type: string
