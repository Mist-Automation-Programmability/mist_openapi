properties:
  device_ids:
    $ref: '../schemas/upgrade-fpga-multi-device-ids.yml'
  models:
    $ref: '../schemas/upgrade-fpga-multi-models.yml'
  reboot:
    default: false
    description: Reboot device immediately after upgrade is completed
    type: boolean
  version:
    description: specific FPGA version
    example: REV37
    type: string
title: Upgrade_Fpga_Multi
type: object
