properties:
  device_ids:
    $ref: '../schemas/upgrade_fpga_multi_device_ids.yml'
  models:
    $ref: '../schemas/upgrade_fpga_multi_models.yml'
  reboot:
    default: false
    description: Reboot device immediately after upgrade is completed
    type: boolean
  version:
    description: specific FPGA version
    example: REV37
    type: string
title: Upgrade_Fpga_Multi
type: object
