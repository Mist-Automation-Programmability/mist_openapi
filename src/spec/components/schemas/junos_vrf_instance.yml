example:
  extra_routes:
    0.0.0.0/0:
      via: 192.168.31.1
  networks:
  - guest
properties:
  extra_routes:
    additionalProperties:
      properties:
        via:
          format: ipv4
          type: string
      type: object
    description: Property key is a CIDR
    type: object
  networks:
    items:
      type: string
    type: array
type: object
