description: list of hostnames without http(s):// (matched by substring), this takes
  precedence over portal_allowed_hostnames
example:
- msg.snapchat.com
items:
  type: string
title: Wlan-Portal-Denied-Hostnames
type: array
