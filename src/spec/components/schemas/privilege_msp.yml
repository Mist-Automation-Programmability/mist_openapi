description: Privilieges settings
properties:
  org_id:
    description: if `scope`==`org`
    format: uuid
    type: string
  org_name:
    description: name of the org (for a site belonging to org)
    type: string
  orggroup_id:
    description: if `scope`==`orggroup`
    format: uuid
    type: string
  role:
    $ref: '../schemas/privilege-msp-role.yml'
  scope:
    $ref: '../schemas/privilege-msp-scope.yml'
  views:
    $ref: '../schemas/privilege-msp-view.yml'
required:
- role
- scope
title: Privilege_Msp
type: object
