description: you can define some URLs that's critical to site operaitons the latency
  will be captured and considered for site health
properties:
  enabled:
    default: true
    type: boolean
  monitors:
    $ref: '../schemas/site_setting_critical_url_monitoring_monitors.yml'
title: Site_Setting_Critical_Url_Monitoring
type: object
