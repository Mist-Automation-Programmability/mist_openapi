description: "schedule to preempt ap\u2019s which are not connected to preferred peer"
properties:
  day_of_week:
    $ref: '../schemas/day_of_week.yml'
  enabled:
    default: false
    description: whether auto preemption should happen
    type: boolean
  time_of_day:
    $ref: '../schemas/time_of_day.yml'
title: Auto_Preemption
type: object
