default: mac
description: 'enum: `device_mac`, `mac`, `port_id`, `site_id`, `type`, `vlan`'
enum:
- device_mac
- mac
- port_id
- site_id
- type
- vlan
title: Org_Wired_Clients_Count_Distinct
type: string
