default: big_bang
description: 'enum: `big_bang` (upgrade all at once), `canary`, `rrm`, `serial` (one
  at a time)'
enum:
- big_bang
- canary
- rrm
- serial
title: Device_Upgrade_Strategy
type: string
