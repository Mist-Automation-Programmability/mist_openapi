properties:
  address:
    description: if `protocol`==`icmp`
    example: 192.168.1.1
    type: string
  app_type:
    type: string
  hostname:
    description: if `protocol`==`http`
    example:
    - www.abc.com
    items:
      type: string
    type: array
  name:
    example: pos_app
    type: string
  network:
    example: lan
    type: string
  protocol:
    default: http
    enum:
    - http
    - udp
    type: string
  url:
    description: if `protocol`==`http`
    example: www.abc.com
    type: string
  vrf:
    example: lan
    type: string
title: Site_Setting_Gateway_Mgmt_App_Probing_Custom_App
type: object
