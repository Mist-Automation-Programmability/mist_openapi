description: ping webhook
properties:
  events:
    items:
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        site_id:
          format: uuid
          type: string
        timestamp:
          type: number
      required:
      - id
      - name
      - site_id
      - timestamp
      type: object
    minItems: 1
    type: array
    uniqueItems: true
  topic:
    default: ping
    type: string
required:
- events
- topic
type: object
