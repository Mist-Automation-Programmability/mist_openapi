description: ping webhook
properties:
  events:
    items:
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        site_id:
          $ref: '../schemas/site_id.yml'
        timestamp:
          type: number
      required:
      - id
      - name
      - site_id
      - timestamp
      title: webhook_ping_event
      type: object
    minItems: 1
    type: array
    uniqueItems: true
  topic:
    default: ping
    type: string
required:
- events
- topic
title: Webhook_Ping
type: object
