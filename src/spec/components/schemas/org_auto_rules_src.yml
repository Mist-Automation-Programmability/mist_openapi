description: 'enum: `dns_suffix`, `lldp_port_desc`, `lldp_system_name`, `model`, `name`,
  `subnet`'
enum:
- dns_suffix
- lldp_port_desc
- lldp_system_name
- model
- name
- subnet
title: Org_Auto_Rules_Src
type: string
