properties:
  contents:
    $ref: '../schemas/syslog-server-contents.yml'
  explicit_priority:
    type: boolean
  facility:
    $ref: '../schemas/syslog-server-facility.yml'
  host:
    example: syslogd.internal
    type: string
  match:
    example: '!alarm|ntp|errors.crc_error[chan]'
    type: string
  port:
    default: 514
    type: integer
  protocol:
    $ref: '../schemas/syslog-server-protocol.yml'
  routing_instance:
    example: routing-instance-name
    type: string
  severity:
    $ref: '../schemas/syslog-server-severity.yml'
  source_address:
    description: if source_address is configured, will use the vlan firstly otherwise
      use source_ip
    type: string
  structured_data:
    type: boolean
  tag:
    type: string
title: syslog_server
type: object
