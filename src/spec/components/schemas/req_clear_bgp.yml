properties:
  neighbor:
    description: neighbor ip-address or 'all'
    type: string
  type:
    default: hard
    enum:
    - hard
    - soft
    - in
    - out
    type: string
  vrf:
    description: vrf name
    type: string
required:
- neighbor
- type
type: object
