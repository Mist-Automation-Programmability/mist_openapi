description: a JSON blob for wall definition (same format as wayfinding_path)
properties:
  coordinate:
    example: actual
    type: string
  nodes:
    $ref: '../schemas/map_wall_path_nodes.yml'
title: Map_Wall_Path
type: object
