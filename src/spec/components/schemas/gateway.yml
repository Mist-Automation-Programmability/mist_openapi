description: device gateway
properties:
  additional_config_cmds:
    $ref: '../schemas/strings.yml'
  created_time:
    readOnly: true
    type: number
  deviceprofile_id:
    format: uuid
    type: string
  dhcpd_config:
    $ref: '../schemas/junos_dhcpd_config.yml'
  extra_routes:
    $ref: '../schemas/gateway_extra_route.yml'
  for_site:
    readOnly: true
    type: boolean
  id:
    format: uuid
    readOnly: true
    type: string
  image1_url:
    type: string
  image2_url:
    type: string
  image3_url:
    type: string
  ip_config:
    $ref: '../schemas/gateway_ip_config.yml'
  managed:
    type: boolean
  modified_time:
    readOnly: true
    type: number
  msp_id:
    format: uuid
    readOnly: true
    type: string
  name:
    type: string
  networks:
    additionalProperties:
      $ref: '../schemas/gateway-networks-additional-properties.yml'
    description: Property key is the network name or a CIDR
    minProperties: 0
    type: object
  ntp_servers:
    $ref: '../schemas/strings.yml'
  oob_ip_config:
    $ref: '../schemas/junos_oob_ip_config.yml'
  org_id:
    $ref: '../schemas/org_id.yml'
  port_config:
    additionalProperties:
      $ref: '../schemas/gateway_port_config.yml'
    description: Property key is the port name or range (e.g. "ge-0/0/0-10")
    type: object
  port_mirroring:
    $ref: '../schemas/gateway-port-mirroring.yml'
  site_id:
    $ref: '../schemas/site_id.yml'
  vars:
    $ref: '../schemas/vars.yml'
title: Gateway
type: object
