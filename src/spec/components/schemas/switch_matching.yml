description: Switch template
properties:
  enable:
    type: boolean
  rules:
    items:
      $ref: '../schemas/switch_matching_rule.yml'
    minItems: 1
    type: array
    uniqueItems: true
title: Switch_Matching
type: object
