properties:
  download_requested:
    $ref: '../schemas/response-device-upgrade-counts-download-requested.yml'
  downloaded:
    $ref: '../schemas/response-device-upgrade-counts-downloaded.yml'
  failed:
    $ref: '../schemas/response-device-upgrade-counts-failed.yml'
  reboot_in_progress:
    $ref: '../schemas/response-device-upgrade-counts-reboot-in-progress.yml'
  rebooted:
    $ref: '../schemas/response-device-upgrade-counts-rebooted.yml'
  scheduled:
    $ref: '../schemas/response-device-upgrade-counts-scheduled.yml'
  skipped:
    $ref: '../schemas/response-device-upgrade-counts-skipped.yml'
  total:
    description: count of devices part of this upgrade
    type: integer
  upgraded:
    description: count of devices which have upgraded successfully
    type: integer
readOnly: true
title: Response-Device-Upgrade-Counts
type: object
