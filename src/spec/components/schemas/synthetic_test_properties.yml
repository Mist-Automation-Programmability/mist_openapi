properties:
  custom_test_urls:
    $ref: '../schemas/synthetic-test-properties-custom-test-urls.yml'
  disabled:
    default: false
    description: for some vlans where we don't want this to run
    type: boolean
  vlan_ids:
    $ref: '../schemas/synthetic-test-properties-vlan-ids.yml'
title: Synthetic_Test_Properties
type: object
