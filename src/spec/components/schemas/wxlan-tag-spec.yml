properties:
  port_range:
    default: '0'
    description: matched dst port, "0" means any
    type: string
  protocol:
    default: any
    description: tcp / udp / icmp / gre / any / ":protocol_number", `protocol_number`
      is between 1-254
    type: string
  subnets:
    $ref: '../schemas/wxlan-tag-spec-subnets.yml'
title: wxlan_tag_spec
type: object
