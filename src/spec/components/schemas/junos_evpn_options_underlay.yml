properties:
  as_base:
    example: 65001
    type: integer
  routed_id_prefix:
    example: /24
    type: string
  subnet:
    description: underlay subnet, by default, `10.255.240.0/20`, or `fd31:5700::/64`
      for ipv6
    example: 10.255.240.0/20
    type: string
  use_ipv6:
    default: false
    description: if v6 is desired for underlay
    type: boolean
title: Junos_Evpn_Options_Underlay
type: object
