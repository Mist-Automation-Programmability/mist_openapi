properties:
  mac:
    description: Required if `op`==`add` or `op`==`preprovision`.
    type: string
  member:
    description: Required if `op`==`remove` or `op`==`preprovision`. Optional if `op`==`add`
    type: integer
  vc_ports:
    $ref: '../schemas/virtual_chassis_member_update_vc_ports.yml'
  vc_role:
    $ref: '../schemas/virtual_chassis_member_update_vc_role.yml'
title: Virtual_Chassis_Member_Update
type: object
