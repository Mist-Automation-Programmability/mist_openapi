additionalProperties:
  $ref: '../schemas/gateway_port_vpn_path.yml'
properties:
  preference:
    description: for a given VPN, when path_selection.strategy=simple, the preference
      for a path (lower is preferred)
    type: integer
title: Gateway_Port_Vpn_Paths
type: object
