additionalProperties:
  $ref: '../schemas/gateway_vrf_instance.yml'
description: Property key is the network name
example:
  CORP_VRF:
    networks:
    - CORP_NET
    - MGMT_NET
title: Gateway_Vrf_Instances
type: object
