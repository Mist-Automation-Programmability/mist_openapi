properties:
  terms:
    description: zero or more criteria/filter can be specified to match the term,
      all criteria have to be met
    items:
      $ref: '../schemas/gateway_routing_policy_item.yml'
    type: array
    uniqueItems: true
title: Gateway_Routing_Policy
type: object
