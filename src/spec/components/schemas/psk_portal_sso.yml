description: if `auth`==`sso`
properties:
  allowed_roles:
    $ref: '../schemas/psk_portal_sso_allowed_roles.yml'
  idp_cert:
    type: string
  idp_sign_algo:
    type: string
  idp_sso_url:
    type: string
  issuer:
    type: string
  nameid_format:
    type: string
  role_mapping:
    additionalProperties:
      type: string
    description: Property key is the role name, property value is the SSO Attribute
    type: object
  use_sso_role_for_psk_role:
    description: if enabled, the `role` above will be ignored
    type: boolean
title: Psk_Portal_Sso
type: object
