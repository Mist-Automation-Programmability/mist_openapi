description: 'Both vc_role master and backup will be matched to routing-engine role
  in Junos preprovisioned VC config. enum: `backup`, `linecard`, `master`'
enum:
- backup
- linecard
- master
title: Switch_Virtual_Chassis_Member_Vc_Role
type: string
