default: big_bang
description: "enum:\n  * `big_bang`: upgrade all at once\n  * `serial`: one at a time.\
  \ enum: `big_bang`, `serial`'"
enum:
- big_bang
- serial
title: Mxedge_Upgrade_Strategy
type: string
