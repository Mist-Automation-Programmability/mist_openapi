description: number of failures allowed within each phase. Only applicable for canary.
  Array length should be same as `canary_phases``. Will be used if provided, else
  max_failure_percentage will be used.
items:
  type: integer
title: Upgrade-Org-Devices-Max-Failures
type: array
