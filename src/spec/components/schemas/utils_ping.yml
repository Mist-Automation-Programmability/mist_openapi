properties:
  count:
    default: 10
    type: integer
  egress_interface:
    description: Interface through which packet needs to egress
    type: string
  host:
    example: 1.1.1.1
    format: ipv4
    type: string
  node:
    $ref: '../schemas/ha_cluster_node_enum.yml'
  size:
    default: 56
    maximum: 65535
    minimum: 56
    type: integer
required:
- host
title: Utils_Ping
type: object
