properties:
  end:
    type: integer
  limit:
    type: integer
  next:
    description: the link to query next set of results. value is null if no next page
      exists.
    type: string
  results:
    $ref: '../schemas/response_rrm_neighbors_results.yml'
  start:
    type: integer
required:
- end
- limit
- results
- start
title: Response_Rrm_Neighbors
type: object
