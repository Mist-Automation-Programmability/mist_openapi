description: list of [x, y, mean]s, x/y are in meters (UI would need to use map.ppm
  to calulate the pixel location from top-left).
items:
  $ref: '../schemas/response-location-coverage-beams-means-items.yml'
title: Response-Location-Coverage-Beams-Means
type: array
