properties:
  aps:
    $ref: '../schemas/webhook-alarm-event-aps.yml'
  bssids:
    $ref: '../schemas/webhook-alarm-event-bssids.yml'
  count:
    description: If present, represents number of events of given type occurred in
      current interval, default=1
    readOnly: true
    type: integer
  event_id:
    description: event id
    format: uuid
    readOnly: true
    type: string
  for_site:
    readOnly: true
    type: boolean
  id:
    format: uuid
    readOnly: true
    type: string
  last_seen:
    readOnly: true
    type: number
  node:
    $ref: '../schemas/ha_cluster_node_enum.yml'
  org_id:
    $ref: '../schemas/org_id.yml'
  site_id:
    $ref: '../schemas/site_id.yml'
  ssids:
    $ref: '../schemas/webhook-alarm-event-ssids.yml'
  timestamp:
    readOnly: true
    type: integer
  type:
    description: event type
    readOnly: true
    type: string
  update:
    description: If presents, represents that this is an update to event with given
      id sent earlier. default=false
    readOnly: true
    type: boolean
required:
- id
- last_seen
- org_id
- site_id
- timestamp
- type
title: webhook_alarm_event
type: object
