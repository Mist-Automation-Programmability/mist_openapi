properties:
  mac:
    description: when replacing a noce, either mac has to remain the same as existing
      cluster
    minLength: 1
    type: string
required:
- mac
title: Gateway_Cluster_Form_Node
type: object
