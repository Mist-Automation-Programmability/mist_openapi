description: "if \n- `type`==`subnet` \n- `type`==`resource` (optional. default is\
  \ `any`)\n- `type`==`static_gbp` if from matching subnet"
items:
  type: string
title: Acl_Tag_Subnets
type: array
