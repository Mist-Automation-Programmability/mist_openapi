properties:
  nodes:
    items:
      properties:
        mac:
          description: when replacing a noce, either mac has to remain the same as
            existing cluster
          minLength: 1
          type: string
      required:
      - mac
      title: gateway_cluster_node
      type: object
    minItems: 1
    type: array
    uniqueItems: true
required:
- nodes
title: Gateway_Cluster_Form
type: object
