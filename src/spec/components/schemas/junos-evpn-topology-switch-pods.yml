description: "by default, core switches are assumed to be connecting all pods. \n\
  if you want to limit the pods, you can specify pods."
items:
  type: integer
title: Junos-Evpn-Topology-Switch-Pods
type: array
