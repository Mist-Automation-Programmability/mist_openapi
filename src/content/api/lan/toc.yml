toc:
- group: CONFIGURE
  items:
  - group: Orgs
    items:
    - group: EVPN Topologies
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: createOrgEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: deleteOrgEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: getOrgEvpnTolopogy
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: listOrgEvpnTopologies
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: updateOrgEvpnTopology
        from: endpoint
    - group: Network Templates
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: createOrgNetworkTemplate
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: deleteOrgNetworkTemplate
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: getOrgNetworkTemplate
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: listOrgNetworkTemplates
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: updateOrgNetworkTemplates
        from: endpoint
  - group: Sites
    items:
    - group: Devices
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices
        endpoint-name: getSiteDeviceConfigCmd
        from: endpoint
    - group: Devices - Wired
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: clearAllLearnedMacsFromPortOnSwitch
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: clearBpduErrosFromPortsOnSwitch
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: deleteSiteLocalSwitchPortConfig
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: pollSiteSwitchStats
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: updateSiteLocalSwitchPortConfig
        from: endpoint
    - group: Devices - Wired Virtual Chassis
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: createSiteVirtualChassis
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: deleteSiteVirtualChassis
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: getSiteDeviceVirtualChassis
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: setSiteVcPort
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: updateSiteVirtualChassisMember
        from: endpoint
    - group: Devices - Wireless
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices - Wireless
        endpoint-name: zeroizeSiteFipsAllAps
        from: endpoint
    - group: Devices Upgrades
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices Upgrades
        endpoint-name: upgradeSiteDeviceBios
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices Upgrades
        endpoint-name: upgraseSiteMultipleDevicesBios
        from: endpoint
    - group: Devices Utilities
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Devices Utilities
        endpoint-name: createSiteDeviceSnapshot
        from: endpoint
    - group: EVPN Topologies
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: createSiteEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: deleteSiteEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: getSiteEvpnTolopogy
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: getSiteEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: updateSiteEvpnTopology
        from: endpoint
    - group: Network Templates
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Network Templates
        endpoint-name: getSiteNetworkTemplateDerived
        from: endpoint
    - group: Synthetic Tests
      items:
      - generate:
        endpoint-group: tag:LAN:CONFIGURE:Sites:Synthetic Tests
        endpoint-name: startSiteSwitchRadiusSyntheticTest
        from: endpoint
- group: MONITOR
  items:
  - group: Orgs
    items:
    - group: Clients - Wired
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Orgs:Clients - Wired
        endpoint-name: countOrgWiredClients
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Orgs:Clients - Wired
        endpoint-name: searchOrgWiredClients
        from: endpoint
    - group: Devices Stats
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Orgs:Devices Stats
        endpoint-name: searchOrgSwOrGwPorts
        from: endpoint
    - group: Stats
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Orgs:Stats
        endpoint-name: countOrgSwitchPorts
        from: endpoint
  - group: Sites
    items:
    - group: Clients - Wired
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Clients - Wired
        endpoint-name: countSiteWiredClients
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Clients - Wired
        endpoint-name: searchSiteWiredClients
        from: endpoint
    - group: Devices - Wired
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Devices - Wired
        endpoint-name: getSiteSwitchesMetrics
        from: endpoint
    - group: Devices Stats
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: countSiteSwOrGwPorts
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: countSiteSwitchPorts
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: searchSiteSwOrGwPorts
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: searchSiteSwitchPorts
        from: endpoint
    - group: Stats
      items:
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Stats
        endpoint-name: countSiteDiscoveredSwitches
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Stats
        endpoint-name: getSiteDiscoveredSwitchesMetrics
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Stats
        endpoint-name: searchSiteDiscoveredSwitches
        from: endpoint
      - generate:
        endpoint-group: tag:LAN:MONITOR:Sites:Stats
        endpoint-name: searchSiteDiscoveredSwitchesMetrics
        from: endpoint
