toc:
- group: Configure
  items:
  - group: Lan
    items:
    - generate:
      endpoint-group: op:LAN:CONFIGURE:LAN
      endpoint-name: upgradeSiteMultipleDevicesBios
      from: endpoint
  - group: Orgs
    items:
    - group: Evpn Topologies
      items:
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: createOrgEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: deleteOrgEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: getOrgEvpnTolopogy
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: listOrgEvpnTopologies
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:EVPN Topologies
        endpoint-name: updateOrgEvpnTopology
        from: endpoint
    - group: Network Templates
      items:
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: createOrgNetworkTemplate
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: deleteOrgNetworkTemplate
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: getOrgNetworkTemplate
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: listOrgNetworkTemplates
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Orgs:Network Templates
        endpoint-name: updateOrgNetworkTemplates
        from: endpoint
  - group: Sites
    items:
    - group: Devices - Wired
      items:
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: deleteSiteLocalSwitchPortConfig
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired
        endpoint-name: updateSiteLocalSwitchPortConfig
        from: endpoint
    - group: Devices - Wired Virtual Chassis
      items:
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: createSiteVirtualChassis
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: deleteSiteVirtualChassis
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: getSiteDeviceVirtualChassis
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: setSiteVcPort
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Devices - Wired Virtual Chassis
        endpoint-name: updateSiteVirtualChassisMember
        from: endpoint
    - group: Evpn Topologies
      items:
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: createSiteEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: deleteSiteEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: getSiteEvpnTolopogy
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: getSiteEvpnTopology
        from: endpoint
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:EVPN Topologies
        endpoint-name: updateSiteEvpnTopology
        from: endpoint
    - group: Network Templates
      items:
      - generate:
        endpoint-group: op:LAN:CONFIGURE:Sites:Network Templates
        endpoint-name: getSiteNetworkTemplateDerived
        from: endpoint
- group: Monitor
  items:
  - group: Orgs
    items:
    - group: Clients - Wired
      items:
      - generate:
        endpoint-group: op:LAN:MONITOR:Orgs:Clients - Wired
        endpoint-name: countOrgWiredClients
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Orgs:Clients - Wired
        endpoint-name: searchOrgWiredClients
        from: endpoint
    - group: Devices Stats
      items:
      - generate:
        endpoint-group: op:LAN:MONITOR:Orgs:Devices Stats
        endpoint-name: countOrgSwitchPorts
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Orgs:Devices Stats
        endpoint-name: searchOrgSwOrGwPorts
        from: endpoint
  - group: Sites
    items:
    - group: Clients - Wired
      items:
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Clients - Wired
        endpoint-name: countSiteWiredClients
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Clients - Wired
        endpoint-name: searchSiteWiredClients
        from: endpoint
    - group: Devices - Wired
      items:
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Devices - Wired
        endpoint-name: getSiteSwitchesMetrics
        from: endpoint
    - group: Devices Stats
      items:
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: countSiteSwOrGwPorts
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: countSiteSwitchPorts
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: searchSiteSwOrGwPorts
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Devices Stats
        endpoint-name: searchSiteSwitchPorts
        from: endpoint
    - group: Discovered Switches Stats
      items:
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Discovered Switches Stats
        endpoint-name: countSiteDiscoveredSwitches
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Discovered Switches Stats
        endpoint-name: getSiteDiscoveredSwitchesMetrics
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Discovered Switches Stats
        endpoint-name: searchSiteDiscoveredSwitches
        from: endpoint
      - generate:
        endpoint-group: op:LAN:MONITOR:Sites:Discovered Switches Stats
        endpoint-name: searchSiteDiscoveredSwitchesMetrics
        from: endpoint
- group: Utilities
  items:
  - group: Lan
    items:
    - generate:
      endpoint-group: op:LAN:UTILITIES:LAN
      endpoint-name: cableTestFromSwitch
      from: endpoint
    - generate:
      endpoint-group: op:LAN:UTILITIES:LAN
      endpoint-name: createSiteDeviceSnapshot
      from: endpoint
    - generate:
      endpoint-group: op:LAN:UTILITIES:LAN
      endpoint-name: pollSiteSwitchStats
      from: endpoint
    - generate:
      endpoint-group: op:LAN:UTILITIES:LAN
      endpoint-name: portsBounceFromSwitch
      from: endpoint
    - generate:
      endpoint-group: op:LAN:UTILITIES:LAN
      endpoint-name: upgradeSiteDeviceBios
      from: endpoint
  - group: Wired
    items:
    - generate:
      endpoint-group: op:LAN:UTILITIES:Wired
      endpoint-name: clearAllLearnedMacsFromPortOnSwitch
      from: endpoint
    - generate:
      endpoint-group: op:LAN:UTILITIES:Wired
      endpoint-name: clearBpduErrosFromPortsOnSwitch
      from: endpoint
