toc:
- group: CONFIGURE
  items:
  - group: Orgs
    items:
    - group: NAC
      items:
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC
        endpoint-name: validateOrgIdpCredential
        from: endpoint
    - group: NAC CRL
      items:
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC CRL
        endpoint-name: deleteOrgNacCrl
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC CRL
        endpoint-name: getOrgNacCrl
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC CRL
        endpoint-name: importOrgNacCrl
        from: endpoint
    - group: NAC Rules
      items:
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Rules
        endpoint-name: createOrgNacRule
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Rules
        endpoint-name: deleteOrgNacRule
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Rules
        endpoint-name: getOrgNacRule
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Rules
        endpoint-name: listOrgNacRules
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Rules
        endpoint-name: updateOrgNacRule
        from: endpoint
    - group: NAC Tags
      items:
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Tags
        endpoint-name: createOrgNacTag
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Tags
        endpoint-name: deleteOrgNacTag
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Tags
        endpoint-name: getOrgNacTag
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Tags
        endpoint-name: listOrgNacTags
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:CONFIGURE:Orgs:NAC Tags
        endpoint-name: updateOrgNacTag
        from: endpoint
- group: MONITOR
  items:
  - group: Orgs
    items:
    - group: Clients - NAC
      items:
      - generate:
        endpoint-group: tag:NAC:MONITOR:Orgs:Clients - NAC
        endpoint-name: countOrgNacClientEvents
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:MONITOR:Orgs:Clients - NAC
        endpoint-name: countOrgNacClients
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:MONITOR:Orgs:Clients - NAC
        endpoint-name: searchOrgNacClientEvents
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:MONITOR:Orgs:Clients - NAC
        endpoint-name: searchOrgNacClients
        from: endpoint
  - group: Sites
    items:
    - group: Clients - NAC
      items:
      - generate:
        endpoint-group: tag:NAC:MONITOR:Sites:Clients - NAC
        endpoint-name: countSiteNacClientEvents
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:MONITOR:Sites:Clients - NAC
        endpoint-name: countSiteNacClients
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:MONITOR:Sites:Clients - NAC
        endpoint-name: searcSitegNacClientEvents
        from: endpoint
      - generate:
        endpoint-group: tag:NAC:MONITOR:Sites:Clients - NAC
        endpoint-name: searchSiteNacClients
        from: endpoint
