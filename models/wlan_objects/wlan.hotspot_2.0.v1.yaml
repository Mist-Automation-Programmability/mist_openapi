description: ''
type: object
properties:
  enabled:
    type: boolean
    description: |
      whether to enable hotspot 2.0 config
  operators:
    type: array
    description: |
      list of operators to support, options: att, google, tmobile, charter, boingo, default is att
    items:
      type:
        - object
        - string
  venue_name:
    type: string
    minLength: 1
    description: |
      venue name, default is site name
