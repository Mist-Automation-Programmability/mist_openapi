description: ''
type: object
x-examples:
  example-1:
    enabled: true
    host: aero.pvt.net
properties:
  enabled:
    type: boolean
    description: whether to enable aeroscout config
  host:
    type: string
    minLength: 1
    description: required if enabled, aeroscout server host
