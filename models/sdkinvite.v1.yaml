title: sdkinvite
type: object
properties:
  name:
    type: string
    description: 'name, will show up in mobile'
  quota_limited:
    type: boolean
    description: 'whether quota limiting is enabled , default is False'
  quota:
    type: integer
    description: number of time this invite can be used
  enabled:
    type: boolean
    description: default is true
required:
  - name
x-examples:
  example-1:
    name: string
    quota_limited: true
    quota: 0
    enabled: true
description: SDK invite request body
