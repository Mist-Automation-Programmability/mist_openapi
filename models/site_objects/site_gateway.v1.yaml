description: Gateway Site settings
type: object
x-examples:
  example-1:
    app_usage: true
properties:
  app_usage:
    type: boolean
    description: 'consumes uplink bandwidth, requires WA license'
  config_revert_timer:
    type: number
    description: 'he rollback timer for commit confirmed. Default is 10, valid range should be 1-30'
    minimum: 1
    maximum: 30
    default: 10
