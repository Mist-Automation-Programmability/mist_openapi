type: object
properties:
  overlay:
    type: object
    properties:
      as:
        type: integer
        description: 'optional, these are defaults'
  underlay:
    type: object
    properties:
      as_base:
        type: integer
      subnet:
        type: string
description: EVPN Options
