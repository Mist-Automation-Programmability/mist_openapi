title: junos.gateway_zone
type: object
properties:
  to_host:
    type: object
    properties:
      protocols:
        type: array
        items:
          type: string
      system_services:
        type: array
        items:
          type: string
      allow_all:
        type: boolean
  to_self:
    type: object
    properties:
      protocols:
        type: array
        items:
          type: string
      system_services:
        type: array
        items:
          type: string
      allow_all:
        type: boolean
  to:
    anyOf:
      - properties:
          nat:
            type: boolean
          web_filtering:
            type: boolean
    type: object
x-examples:
  example-1:
    to_host:
      protocols:
        - all
      system_services:
        - all
    to_self:
      allow_all: true
    to:
      untrust:
        nat: true
        web_filtering: true
